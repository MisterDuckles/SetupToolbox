<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="WingetAppDeployer_WinUI.Pages.TweaksPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <Grid Padding="36,24,36,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Tweaks"
                   Style="{StaticResource TitleTextBlockStyle}"
                   Margin="0,0,0,8" />

        <InfoBar Grid.Row="1"
                 Severity="Informational"
                 Title="Quick Windows tweaks"
                 Message="Toggle veelgebruikte Windows-instellingen direct hier — geen Settings-app, geen regedit. Status reflecteert je huidige systeem. Iconen rechts tonen of een Explorer-restart, sign-out of reboot nodig is voor zichtbaarheid."
                 IsOpen="True"
                 IsClosable="True"
                 MaxWidth="780"
                 HorizontalAlignment="Left"
                 Margin="0,0,0,12" />

        <!-- Resultaat-bar voor apply/revert feedback. -->
        <InfoBar x:Name="ResultBar"
                 Grid.Row="2"
                 Severity="Success"
                 IsOpen="False"
                 IsClosable="True"
                 MaxWidth="780"
                 HorizontalAlignment="Left"
                 Margin="0,0,0,12" />

        <ScrollView Grid.Row="3"
                    VerticalScrollBarVisibility="Auto"
                    Padding="0,0,14,12"
                    ScrollAnimationStarting="ScrollView_ScrollAnimationStarting">
            <StackPanel x:Name="CategoriesContainer" Spacing="20" />
        </ScrollView>

        <!-- Loading-overlay tijdens initial state-detection. -->
        <Grid x:Name="LoadingOverlay"
              Grid.RowSpan="4"
              Background="{ThemeResource SmokeFillColorDefaultBrush}"
              Visibility="Visible">
            <StackPanel HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Spacing="12">
                <ProgressRing Width="40" Height="40" IsIndeterminate="True" />
                <TextBlock Text="Reading Windows state..."
                           Style="{StaticResource BodyTextBlockStyle}"
                           HorizontalAlignment="Center" />
            </StackPanel>
        </Grid>
    </Grid>
</Page>
