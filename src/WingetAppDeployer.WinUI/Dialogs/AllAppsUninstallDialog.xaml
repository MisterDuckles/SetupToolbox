<?xml version="1.0" encoding="utf-8" ?>
<ContentDialog
    x:Class="WingetAppDeployer_WinUI.Dialogs.AllAppsUninstallDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:dialogs="using:WingetAppDeployer_WinUI.Dialogs"
    mc:Ignorable="d"
    Title="Uninstall apps"
    PrimaryButtonText="Close"
    IsPrimaryButtonEnabled="False"
    DefaultButton="Primary"
    CornerRadius="8"
    PrimaryButtonStyle="{StaticResource DialogPrimaryButtonStyle}">

    <ContentDialog.Resources>
        <x:Double x:Key="ContentDialogMinWidth">680</x:Double>
        <x:Double x:Key="ContentDialogMaxWidth">720</x:Double>
    </ContentDialog.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   x:Name="ProgressHeader"
                   Text="Preparing..."
                   Style="{StaticResource BodyStrongTextBlockStyle}"
                   Margin="0,0,0,8" />

        <TextBlock Grid.Row="1"
                   x:Name="UacHint"
                   Text="A UAC prompt may appear — Store and Other-source apps require administrator rights to uninstall."
                   Style="{StaticResource CaptionTextBlockStyle}"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                   TextWrapping="Wrap"
                   Margin="0,0,0,16" />

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" MaxHeight="420">
            <ItemsControl x:Name="ItemList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="dialogs:AllAppsUninstallEntry">
                        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1"
                                CornerRadius="8"
                                Padding="16,14"
                                Margin="0,0,0,8">
                            <Grid ColumnSpacing="16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="24" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="120" />
                                </Grid.ColumnDefinitions>

                                <Grid Grid.Column="0"
                                      Width="20" Height="20"
                                      VerticalAlignment="Center">
                                    <ProgressRing Width="20" Height="20"
                                                  IsActive="True"
                                                  Visibility="{x:Bind RingVisibility, Mode=OneWay}" />
                                    <FontIcon Glyph="&#xE73E;"
                                              FontFamily="{StaticResource SymbolThemeFontFamily}"
                                              FontSize="18"
                                              Foreground="{ThemeResource SystemFillColorSuccessBrush}"
                                              Visibility="{x:Bind CheckVisibility, Mode=OneWay}" />
                                    <FontIcon Glyph="&#xEA39;"
                                              FontFamily="{StaticResource SymbolThemeFontFamily}"
                                              FontSize="18"
                                              Foreground="{ThemeResource SystemFillColorCriticalBrush}"
                                              Visibility="{x:Bind ErrorVisibility, Mode=OneWay}" />
                                    <FontIcon Glyph="&#xE711;"
                                              FontFamily="{StaticResource SymbolThemeFontFamily}"
                                              FontSize="18"
                                              Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                              Visibility="{x:Bind CancelledVisibility, Mode=OneWay}" />
                                </Grid>

                                <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="6">
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <TextBlock Text="{x:Bind DisplayName}"
                                                   Style="{StaticResource BodyStrongTextBlockStyle}"
                                                   FontSize="15"
                                                   VerticalAlignment="Center" />
                                        <Border Background="{x:Bind SourceBadgeBrush}"
                                                CornerRadius="4"
                                                Padding="6,2"
                                                VerticalAlignment="Center">
                                            <TextBlock Text="{x:Bind SourceBadgeText}"
                                                       Style="{StaticResource CaptionTextBlockStyle}" />
                                        </Border>
                                    </StackPanel>
                                    <ProgressBar HorizontalAlignment="Stretch"
                                                 IsIndeterminate="True"
                                                 ShowPaused="False"
                                                 ShowError="False"
                                                 Visibility="{x:Bind BarVisibility, Mode=OneWay}" />
                                    <TextBlock Text="{x:Bind Message, Mode=OneWay}"
                                               Style="{StaticResource CaptionTextBlockStyle}"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                               TextTrimming="CharacterEllipsis"
                                               Visibility="{x:Bind MessageVisibility, Mode=OneWay}" />
                                </StackPanel>

                                <TextBlock Grid.Column="2"
                                           Text="{x:Bind StateLabel, Mode=OneWay}"
                                           Style="{StaticResource BodyTextBlockStyle}"
                                           Foreground="{x:Bind StateLabelBrush, Mode=OneWay}"
                                           TextAlignment="Right"
                                           VerticalAlignment="Center" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</ContentDialog>
