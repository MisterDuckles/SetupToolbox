<?xml version="1.0" encoding="utf-8" ?>
<ContentDialog
    x:Class="SetupToolbox.Dialogs.SnapshotBrowserDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:loc="using:SetupToolbox.Helpers"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="{loc:Localize Key=dialog.snapshots.title}"
    CloseButtonText="{loc:Localize Key=common.close}"
    DefaultButton="Close"
    CornerRadius="8">

    <Grid MinWidth="520" MinHeight="380" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   TextWrapping="Wrap"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                   Text="{loc:Localize Key=dialog.snapshots.desc}" />

        <ScrollView Grid.Row="1"
                    VerticalScrollBarVisibility="Auto"
                    Padding="0,0,12,0">
            <StackPanel x:Name="SnapshotList" Spacing="8" />
        </ScrollView>

        <InfoBar x:Name="ResultBar"
                 Grid.Row="2"
                 Severity="Informational"
                 IsOpen="False"
                 IsClosable="True" />

        <Grid x:Name="EmptyPanel"
              Grid.Row="1"
              Visibility="Collapsed">
            <StackPanel HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Spacing="8">
                <FontIcon Glyph="&#xE7B8;"
                          FontSize="48"
                          Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
                <TextBlock Text="{loc:Localize Key=dialog.snapshots.none.title}"
                           Style="{StaticResource SubtitleTextBlockStyle}"
                           HorizontalAlignment="Center" />
                <TextBlock Text="{loc:Localize Key=dialog.snapshots.none.desc}"
                           Style="{StaticResource CaptionTextBlockStyle}"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           TextWrapping="Wrap"
                           HorizontalAlignment="Center"
                           MaxWidth="380"
                           TextAlignment="Center" />
            </StackPanel>
        </Grid>
    </Grid>
</ContentDialog>
